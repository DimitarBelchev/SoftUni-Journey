function solve(input){
    let a = Number(input.shift());
    let b = Number(input.shift());
    let c = Number(input.shift());
    let ab = 0;
    if(a == b){ab = a}
    if(ab == c){console.log('yes')}
    else{console.log('no')}
}
    solve([])